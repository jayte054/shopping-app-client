export const PrintData = () => {
    return (
        <>
             <ul>
                        {/* {itemList.map((item, index) => (
                            <li key={index}>{item}</li>
                        ))} */}
                    </ul>
        </>
    )
}